// Toolbar logic is implemented inline in `browser.html` and uses the
// preload-exposed `internalBrowser` API. This file is intentionally a no-op
// to avoid stale iframe-based logic.
console.log('browser.js loaded (no-op)');